[project]
name = "pip-clean"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "packaging>=24.2",
    "pip>=24.3.1",
    "pipfile>=0.0.2",
]

[dependency-groups]
dev = [
    "black>=24.10.0",
    "mypy>=1.13.0",
]
